{
  "name": "react-plus-vue-main",
  "version": "0.0.0",
  "description": "The libraries for using vue features in react",
  "private": true,
  "scripts": {
    "format": "prettier --write . && git add . && git reset"
  },
  "author": "Omer M",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.0.3"
  }
}
